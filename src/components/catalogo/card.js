import { Cartao } from "./catalogo-style.js"

const Card = ({props}) => {
    
    const id = props.id;
    const titulo = props.titulo;
    const imagem = props.imagem;

    return(
        <Cartao>

        </Cartao>
    )
}

export default Card;